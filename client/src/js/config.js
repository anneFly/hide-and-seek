var Config = (function ($, console, window, document, Config) {
	"use strict";

	Config = {
        BLOCK_SIZE: 20,
        PLAYER_START_X: 12,
        PLAYER_START_Y: 12
	};
	
	return Config;
	
}(jQuery, console, this, this.document, Config || {}));
